language: generic
sudo: required
services:
  - docker

before_install:
  - docker build -t sjmcdowall/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true sjmcdowall/docker-react npm run test
